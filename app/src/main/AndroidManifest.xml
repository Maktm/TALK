<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.computerscience.talk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Fitness2"></activity>
        <activity android:name=".Fitness1" />
        <activity android:name=".Article4" />
        <activity android:name=".Article3" />
        <activity android:name=".Article2" />
        <activity android:name=".Article1" />
        <activity android:name=".JournalActivity" />
        <activity android:name=".GraphActivity" />
        <activity android:name=".ResourcesActivity" />
        <activity android:name=".SliderActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>